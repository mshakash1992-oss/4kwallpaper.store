import { db } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function AdminDashboard() { const [wallpapers, drafts, downloads, searches] = await Promise.all([db.wallpaper.count(), db.wallpaper.count({ where: { status: "REVIEW" } }), db.download.count(), db.searchQuery.count()]); const values = [{ label: "Wallpapers", value: wallpapers }, { label: "Awaiting review", value: drafts }, { label: "Downloads", value: downloads }, { label: "Search queries", value: searches }]; return <><p className="eyebrow">Overview</p><h1 className="mt-2 text-3xl font-black">Dashboard</h1><div className="mt-7 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{values.map((item) => <div className="surface p-5" key={item.label}><p className="text-sm text-muted">{item.label}</p><p className="mt-2 text-3xl font-black">{item.value}</p></div>)}</div></>; }
