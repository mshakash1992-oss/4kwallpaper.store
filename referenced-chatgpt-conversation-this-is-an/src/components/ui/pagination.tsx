import Link from "next/link";
export function Pagination({ page, pages, path }: { page: number; pages: number; path: string }) { if (pages <= 1) return null; const join = (p: number) => `${path}${path.includes("?") ? "&" : "?"}page=${p}`; return <nav aria-label="Pagination" className="mt-9 flex justify-between"><Link className="rounded-xl border px-4 py-3 disabled:opacity-50" aria-disabled={page === 1} href={page === 1 ? path : join(page - 1)}>Previous</Link><span className="py-3 text-sm text-muted">Page {page} of {pages}</span><Link className="rounded-xl border px-4 py-3" aria-disabled={page >= pages} href={page >= pages ? path : join(page + 1)}>Next</Link></nav>; }
