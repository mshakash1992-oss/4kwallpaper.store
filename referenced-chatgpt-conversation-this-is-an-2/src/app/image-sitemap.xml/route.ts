import { wallpapers } from "@/lib/data";
import { absoluteUrl } from "@/lib/site";
export function GET() { const urls = wallpapers.map((wallpaper) => `<url><loc>${absoluteUrl(`/wallpapers/${wallpaper.slug}`)}</loc><image:image><image:loc>${wallpaper.imageUrl}</image:loc><image:title>${wallpaper.title}</image:title><image:caption>${wallpaper.altText}</image:caption></image:image></url>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">${urls}</urlset>`, { headers: { "Content-Type": "application/xml", "Cache-Control": "public, max-age=3600" } }); }
